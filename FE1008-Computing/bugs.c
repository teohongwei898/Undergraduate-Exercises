/* Calculate and display the difference of two input values /*
include <stdio.h>
int Main(void)
{
int X, x, sum;
scanf("%d", & X);
scanf("%f", & x);
X + x = sum;
printf("The sum of %d and %d is %d\n", X, x, sum);
return 0;
